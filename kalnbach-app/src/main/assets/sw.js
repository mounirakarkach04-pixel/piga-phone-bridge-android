const CACHE='kalnbach-static-v7-anton-rc2';
const STATIC=['./','./index.html','./app.css','./enterprise.css','./production.css','./app.js','./enterprise-ui.js','./auth-enhancements.js','./standalone-bootstrap.js','./release-finalization.js','./production-hardening.js','./kalnbach-logo.svg','./manifest.json'];
self.addEventListener('install',event=>{event.waitUntil(caches.open(CACHE).then(cache=>cache.addAll(STATIC)).then(()=>self.skipWaiting()))});
self.addEventListener('activate',event=>{event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k!==CACHE).map(k=>caches.delete(k)))).then(()=>self.clients.claim()))});
self.addEventListener('fetch',event=>{const req=event.request,url=new URL(req.url);if(req.method!=='GET')return;if(url.origin!==self.location.origin)return;if(url.pathname.includes('/auth/')||url.pathname.includes('/rest/'))return;event.respondWith(fetch(req).then(res=>{if(res.ok&&(req.destination==='document'||req.destination==='script'||req.destination==='style'||req.destination==='image'||req.destination==='manifest')){const copy=res.clone();caches.open(CACHE).then(c=>c.put(req,copy))}return res}).catch(()=>caches.match(req).then(hit=>hit||caches.match('./index.html'))))});
